// @ts-check

/**
 * @typedef {{
 *   project: (p: { x: number; y: number; z: number }) => { x: number; y: number; scale: number } | null
 * }} CameraLike
 */

/**
 * Referencia vacía para forzar que el archivo sea tratado como un módulo ESM.
 */
export const _PrimitiveTypes = {};
