// @ts-check

/**
 * @typedef {import("../../canvasOrganicBlob.js").CameraLike} CameraLike
 */

/**
 * @typedef {{
 *   alpha: number;
 *   center: { x: number; y: number; z: number };
 *   crest: { x: number; y: number; z: number };
 *   elapsed: number;
 *   enemy: import("../../../game/entities/Enemy.js").Enemy;
 *   facing: { x: number; y: number; z: number };
 *   gelPhase: number;
 *   hornOuter: string;
 *   hornSheen: string;
 *   side: { x: number; y: number; z: number };
 *   toxicHull: string;
 *   toxicRim: string;
 * }} FiendRenderState
 */

/** @type {void} */
export const SlimeTypes = undefined;
